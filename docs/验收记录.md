# 本地验收记录

日期：2026-09-12。

## 验证环境

- macOS arm64，项目内独立 Node.js 24.21.0。
- Express 5.2.1、EJS 3.1.10、better-sqlite3 13.0.3，SQLite 3.53.4。
- 项目开始时为空目录且没有 Git 仓库；未覆盖已有项目代码或修改系统默认 Node。
- HTTP 测试使用临时目录和真实 SQLite；浏览器验收使用独立数据库及临时共享文件，正式本地数据库保留内置默认账号。

## 自动验证

`npm run check`：26 个 JavaScript/EJS 文件检查通过，未构建前端。

`npm test`：15 项通过，0 项失败，0 项跳过。覆盖：

1. 双账号默认密码、重复初始化及重启保持密码和状态。
2. 普通用户无改密入口，直接调用改密、日志及账号管理接口被拒绝。
3. 管理员修改普通用户和本人密码，撤销所有旧会话，并拦截并发旧请求重新写回会话。
4. 创建账号、用户名唯一性、账号启停和最后一个管理员保护。
5. CSRF、Cookie 属性、退出、固定会话到期和重启持久化。
6. 登录成功、失败、限流、审计字段，以及不可信代理头处理。
7. 可信代理取 IP 和 HTTPS Secure Cookie。
8. 配置目录、数据库目录及符号链接边界。
9. 管理员日志筛选、北京时间边界、分页、HTML 转义和 SQL 参数化。
10. 实时目录读取、文件夹优先、中文筛选、分页、空目录、刷新及文件名转义。
11. 未认证下载、路径穿越、绝对路径、多重编码、目录及越界链接拦截。
12. 中文特殊文件名、零字节文件、下载内容一致、Range 返回整文件以及 HEAD 不记账。
13. 下载审计写入失败阻止传输、文件删除后记录失败。
14. 128 MiB 大文件流式发送、中断记录及重启修复遗留下载。
15. Unix 文件及目录无读取权限时拒绝访问。

模拟审计写入失败的用例会输出一次“请求处理失败”，对应预期的 503 响应。

## 浏览器验收

在 Codex 浏览器中使用两个内置账号实际登录，检查登录页、文件列表和管理员页面布局。

- admin 登录后可以访问用户管理、登录记录和下载记录。
- 用户管理正确列出两个内置账号；创建账号弹窗和修改密码弹窗显示正常，修改目标为选中的账号。提交和会话撤销逻辑由 HTTP 集成测试验证。
- user 登录后仅显示文件空间，无用户管理、日志或修改密码入口。
- user 逐层进入“项目资料”，通过下载按钮触发中文文件“项目说明.txt”的浏览器下载事件。
- 切回 admin 后，在下载记录页面查到同一次操作，且与 SQLite 原始记录一致：

| 字段 | 结果 |
|---|---|
| 账号 | user |
| 相对路径 | 项目资料/项目说明.txt |
| 文件大小 | 34 B |
| IP | 127.0.0.1 |
| 开始时间 | 2026-09-12T13:17:33.984Z |
| 结束时间 | 2026-09-12T13:17:33.986Z |
| 状态 | completed / 服务端发送完成 |

页面显示为北京时间 2026-09-12 21:17:33；浏览器控制台未发现 error 或 warn。

## 验证边界

本次未部署到真实 Linux 服务器，未现场执行 systemd、Nginx、TLS 证书或外部客户端 IP 联调。已提供配置示例和目标服务器验收步骤。

本地服务仅监听 127.0.0.1，默认文件目录为项目 storage，SQLite 为 data/wangpan.sqlite。临时浏览器目录、数据库及早期失败测试产生的临时文件在收尾清理；保留 node_modules 和项目内 Node.js 运行时供本地使用。
